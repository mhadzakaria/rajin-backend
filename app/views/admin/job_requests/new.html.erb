<h1>New Job Request</h1>

<%= render partial: 'admin/job_requests/form', locals: {job_request: @job_request, url: admin_job_requests_path, method: :post} %>

<%= link_to 'Back', admin_job_requests_path, class: 'btn btn-secondary' %>
