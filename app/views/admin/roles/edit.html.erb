<h1>Editing Role</h1>

<%= render partial: 'admin/roles/partials/form', locals: {role: @role, url: admin_role_path(@role), method: :patch} %>

<%#= link_to 'Show', admin_role_path(@role), class: 'btn btn-primary', data: { disable_with: 'Please wait...' } %>
<%= link_to 'Back', admin_roles_path, class: 'btn btn-secondary', data: { disable_with: 'Please wait...' } %>