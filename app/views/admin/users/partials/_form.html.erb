<div class="card card-default">
   <div class="card-header"></div>
   <div class="card-body">
      <%= form_for user, url: url, method: method, local: true, class: 'form-horizontal' do |form| %>
        <% if user.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

            <ul>
            <% user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <fieldset>
          <legend>Form user</legend>
          <div class="form-group row">
            <%= form.label :nickname, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :nickname, class: 'form-control', type: 'text' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :first_name, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :first_name, class: 'form-control', type: 'text' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :last_name, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :last_name, class: 'form-control', type: 'text' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :phone_number, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :phone_number, class: 'form-control', type: 'text' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :date_of_birth, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :date_of_birth, class: 'form-control', type: 'date' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :gender, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.radio_button :gender, 'female', checked: user.gender.eql?('female') %> Female
              <%= form.radio_button :gender, 'male', checked: user.gender.eql?('male') %> Male
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :full_address, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_area :full_address, class: 'form-control' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :city, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :city, class: 'form-control', type: 'text' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :postcode, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :postcode, class: 'form-control', type: 'number' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :state, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :state, class: 'form-control', type: 'text' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :country, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
              <%= form.text_field :country, class: 'form-control', type: 'text' %>
            </div>
          </div>

          <div class="form-group row">
            <%= form.label :role, class: 'col-md-2 col-form-label' %>
            <div class="col-md-10">
            	<%= form.select(:role_id, Role.all.map {|role| [role.role_name, role.id] }, class: 'form-control') %>
            </div>
          </div>

        <div class="actions">
          <%= form.submit submit_button_label(user), class: 'btn btn-primary' %>
        </div>
      <% end %>
   </div>
</div>